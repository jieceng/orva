# Validator

<NanoVersionBanner version="v3.1" channel="Stable snapshot" updated="2026-04" />

The v3.1 validator layer supports `json`, `form`, `query`, `param`, `header`, `cookie` and `text`, plus zod integration.
