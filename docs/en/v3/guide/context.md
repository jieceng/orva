# Context and Responses

<OrvaVersionBanner version="v3.1" channel="Stable snapshot" updated="2026-04" />

The v3.1 context still exposes:

- `c.req`
- `c.url`
- `c.params`
- `c.query`
- `c.set()` / `c.get()`
- `c.valid()`
- `c.text()` / `c.json()` / `c.html()` / `c.redirect()`
