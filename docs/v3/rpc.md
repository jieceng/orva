# RPC

<NanoVersionBanner version="v3.1" channel="Stable snapshot" updated="2026-04" />

`nano/rpc` 在 v3.1 线上已经支持：

- 基于路由注册表的类型推导
- `param/query/body/header/cookie` 请求选项
- 按 `content-type` 选择 body 序列化策略
